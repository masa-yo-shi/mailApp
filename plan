4/19までは、reactポートフォリオ作成に専念。
4/30までにインターン2社以上受ける。

ポートフォリオ

製造業向けメール分類

つめこみたいアイデア書き出し
2割のメールが8割の利益(自動判定)
メールチェックを定時に　正午、16:00
やり取りを減らすメールテンプレート。もしも～ならば、アジェンダ送ってください、質問対応（別ページ参照）
拡張機能で導入しやすいという点で差別化。

MVP策定
入力：メール（IMAP / Gmail API想定）。
処理：分類（カテゴリ）、担当者割当、対応期限。
代表的なユースケース（見積依頼、納期問い合わせ、クレーム、図面送付、採用など）。
出力：シンプルな一覧画面＋フィルタ＋「次にやること」欄。
管理：アカウント1社・ユーザ数少数前提の簡易認証。

メール→データベース→AIで読み込む（chatgpt）→分類→出力
画面　バイト・バイト以外で表示させる。

データベース→chatpP（in：件名　プロンプト：件名をbaito, tosyokan, othersに分類して out: baito, tosyokan, others）→データベース

categoryカラムを追加
件名を取り出す関数を書く。
inとoutを指定したgptモデルをコーディング
outをデータベースに追加する関数を書く。
データベースのcategoryでフィルタリングしてuiに反映させる。
